'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.default = connectSelect;

var _connectInput = require('./connectInput');

var _connectInput2 = _interopRequireDefault(_connectInput);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var serialize = function serialize(event) {
    var target = event.target;
    var value = target.value;
    var type = target.type;

    if (type === 'select-multiple') {
        var values = [];
        var options = target.options;

        for (var i = 0, l = options.length; i < l; i++) {
            var option = options[i];
            if (option.selected) values.push(option.value);
        }
        return values;
    }
    return value;
};

var mapStateToProps = function mapStateToProps(_ref, _ref2) {
    var value = _ref.value;
    var disabled = _ref.disabled;
    var multiple = _ref2.multiple;
    return {
        value: !value && multiple ? [] : value,
        disabled: disabled
    };
};

function connectSelect(Component) {
    var options = arguments.length <= 1 || arguments[1] === undefined ? {} : arguments[1];

    return (0, _connectInput2.default)(Component, _extends({
        serialize: serialize,
        mapStateToProps: mapStateToProps
    }, options));
}