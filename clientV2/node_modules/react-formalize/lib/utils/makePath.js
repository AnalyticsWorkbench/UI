'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = makePath;
function makePath(path, value) {
    var fragments = path.split('.');
    var obj = {};
    var tmp = obj;

    for (var i = 0, l = fragments.length; i < l; i++) {
        var fragment = fragments[i];
        tmp[fragment] = i === l - 1 ? value : {};
        tmp = tmp[fragment];
    }

    return obj;
}